package com.ticketmaster.discovery.model;

import lombok.Getter;



@Getter
public class DiscoveryErrors { // TODO: Use or delete this class
/*
 * @JsonProperty(value = "errors") private final List<DiscoveryError> errors;
 * 
 * public DiscoveryErrors(String code, String detail, String status, Link... links) { this(new
 * DiscoveryError(code, detail, status, links)); }
 * 
 * public DiscoveryErrors(DiscoveryErrors.DiscoveryError... discoveryErrors) {
 * Assert.notEmpty(discoveryErrors, "Error must not be null"); this.errors =
 * Arrays.asList(discoveryErrors); }
 * 
 * @Getter public static class DiscoveryError extends ResourceSupport {
 * 
 * private final String code;
 * 
 * private final String detail;
 * 
 * private final String status;
 * 
 * public DiscoveryError(String code, String detail, String status, Link... links) {
 * Assert.hasText(code, "Code must not be null or empty!"); Assert.hasText(detail,
 * "Detail must not be null or empty!"); this.code = code; this.detail = detail; this.status =
 * status; this.add(Arrays.asList(links)); } }
 */
}
